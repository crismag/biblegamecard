# Production configuration templates

Copy `model_adapter.yaml` into a production configuration area only after choosing a provider and exact model revision. Adapter data is intentionally outside canonical character and prompt knowledge so semantic prompts remain model-neutral. An adapter remains unusable until its approval state, transformations, limitations, output format, and colour assumptions are reviewed.
